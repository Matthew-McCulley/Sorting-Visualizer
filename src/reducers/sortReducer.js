import {ALGORITHM} from '../actions/types'

const initialState = {
    algorithm: '',
}

export default function (state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}